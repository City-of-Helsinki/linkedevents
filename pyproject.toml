[tool.ruff]
target-version = "py39"

[tool.ruff.lint]
select = [
    # Pyflakes
    "F",
    # Pycodestyle
    "E",
    "W",
    # isort
    "I001"
]
extend-per-file-ignores = { "*migrations*" = ["E501"] }
pycodestyle.max-line-length = 120
